# HabitStreak Production Environment
# Copy this file to .env.production and fill in your values
# NEVER commit .env.production to git!

# ============================================
# REQUIRED: Database
# ============================================
# PostgreSQL password for the 'habitstreak' user
# Generate a strong password: openssl rand -base64 32
POSTGRES_PASSWORD=change_me_to_a_strong_password

# ============================================
# REQUIRED: Auth Secret
# ============================================
# Secret for JWT signing - MUST be at least 32 characters
# Generate with: openssl rand -base64 32
AUTH_SECRET=change_me_to_a_random_string_at_least_32_chars

# ============================================
# OPTIONAL: Application Port
# ============================================
# External port the app will be accessible on
# Default: 3000
# APP_PORT=3000

# ============================================
# OPTIONAL: Timezone
# ============================================
# Default: Europe/Amsterdam (hardcoded in app)
# TZ=Europe/Amsterdam

# ============================================
# NOTE: No URL configuration needed!
# ============================================
# AUTH_TRUST_HOST=true is set in docker-compose.prod.yml
# The app auto-detects the URL from the request Host header
# This means login works on localhost, IP address, or domain
# without any URL configuration.
