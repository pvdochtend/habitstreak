# HabitStreak Production Environment
# Copy this file to .env.production and fill in your values
# NEVER commit .env.production to git!

# ============================================
# REQUIRED: Database
# ============================================
# PostgreSQL password for the 'habitstreak' user
# Generate a strong password: openssl rand -base64 32
POSTGRES_PASSWORD=change_me_to_a_strong_password

# ============================================
# REQUIRED: NextAuth Secret
# ============================================
# Secret for JWT signing - MUST be at least 32 characters
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=change_me_to_a_random_string_at_least_32_chars

# ============================================
# OPTIONAL: NextAuth URL
# ============================================
# NEXTAUTH_URL is optional - the app auto-detects the URL from request headers
# This allows access via localhost, IP address, or domain name without changing config
#
# Only set NEXTAUTH_URL if:
# - You want to force a specific URL
# - You're using HTTPS and want secure cookies (set to https://your-domain.com)
#
# Examples:
# NEXTAUTH_URL=http://192.168.1.100:3000
# NEXTAUTH_URL=https://habits.yourdomain.com
#
# If not set: Works with any URL (localhost, IP, domain)
# If set to https://: Enables secure cookies (recommended for public access)
# If set to http://: Uses that specific URL only

# NEXTAUTH_URL=

# ============================================
# OPTIONAL: Timezone
# ============================================
# Default: Europe/Amsterdam (hardcoded in app)
# TZ=Europe/Amsterdam
