---
phase: 12-login-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/signup/page.tsx
  - src/components/auth/auth-header.tsx
autonomous: true

must_haves:
  truths:
    - "User sees welcoming message on login page"
    - "User sees HabitStreak branding/logo on login page"
    - "Login page visual style matches landing page glassmorphism"
    - "Signup page has same polish as login page"
  artifacts:
    - path: "src/components/auth/auth-header.tsx"
      provides: "Reusable branding header with flame icon"
      min_lines: 20
    - path: "src/app/(auth)/login/page.tsx"
      provides: "Polished login page with glassmorphism"
      contains: "glass-strong"
    - path: "src/app/(auth)/signup/page.tsx"
      provides: "Polished signup page with glassmorphism"
      contains: "glass-strong"
  key_links:
    - from: "src/app/(auth)/login/page.tsx"
      to: "src/components/auth/auth-header.tsx"
      via: "import AuthHeader"
      pattern: "AuthHeader"
    - from: "src/app/(auth)/signup/page.tsx"
      to: "src/components/auth/auth-header.tsx"
      via: "import AuthHeader"
      pattern: "AuthHeader"
---

<objective>
Polish login and signup pages with welcoming message, branding, and glassmorphism styling.

Purpose: Create cohesive visual experience from landing page through authentication flow.
Output: Polished auth pages matching landing page aesthetic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-landing-page/11-01-SUMMARY.md

# Relevant source files:
@src/app/(auth)/login/page.tsx
@src/app/(auth)/signup/page.tsx
@src/components/landing/hero-section.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth header component with branding</name>
  <files>src/components/auth/auth-header.tsx</files>
  <action>
    Create reusable AuthHeader component for auth pages:

    1. Create `src/components/auth/auth-header.tsx` with:
       - Client component (uses animations)
       - Flame icon (Lucide `Flame`) with primary color and subtle animation
       - "HabitStreak" app name in bold text
       - Welcoming message prop for page-specific text
       - Staggered animations matching landing page pattern (fade-in, slide-up)

    2. Component structure:
       ```tsx
       interface AuthHeaderProps {
         title: string;        // e.g., "Welkom terug!" or "Aan de slag!"
         subtitle: string;     // e.g., "Log in op je HabitStreak account"
       }
       ```

    3. Layout:
       - Centered container
       - Flame icon with orange/amber color (text-orange-500) and subtle glow
       - App name below icon
       - Title text (welcoming message)
       - Subtitle text (description)

    4. Use existing animation classes: animate-fade-in, animate-slide-up
    5. Dutch text for user-facing content
  </action>
  <verify>npm run build succeeds without TypeScript errors</verify>
  <done>AuthHeader component exists with flame icon, app name, and welcoming message slots</done>
</task>

<task type="auto">
  <name>Task 2: Polish login page with glassmorphism and branding</name>
  <files>src/app/(auth)/login/page.tsx</files>
  <action>
    Update login page to match landing page visual style:

    1. Import AuthHeader and AnimatedBackground:
       - `import { AuthHeader } from '@/components/auth/auth-header'`
       - `import { AnimatedBackground } from '@/components/backgrounds/animated-background'`

    2. Replace outer container:
       - Remove: `bg-gradient-to-br from-background via-background to-primary/5`
       - Add: `<AnimatedBackground />` as first child
       - Keep: `min-h-screen flex items-center justify-center p-4` but wrap content in relative div

    3. Replace CardHeader content:
       - Remove CardTitle and CardDescription
       - Add AuthHeader with:
         - title: "Welkom terug!"
         - subtitle: "Log in op je HabitStreak account"

    4. Apply glassmorphism to Card:
       - Add: `glass-strong` class
       - Keep: `w-full max-w-md shadow-xl animate-scale-in`

    5. Ensure form and error handling remain unchanged (preserve all existing functionality)

    6. Keep all Dutch text for user-facing content
  </action>
  <verify>npm run build succeeds; login page loads at /login with branding visible</verify>
  <done>Login page shows flame icon, "HabitStreak" name, "Welkom terug!" message, and glassmorphism card</done>
</task>

<task type="auto">
  <name>Task 3: Polish signup page to match login page</name>
  <files>src/app/(auth)/signup/page.tsx</files>
  <action>
    Update signup page with same polish pattern as login:

    1. Import AuthHeader and AnimatedBackground:
       - `import { AuthHeader } from '@/components/auth/auth-header'`
       - `import { AnimatedBackground } from '@/components/backgrounds/animated-background'`

    2. Replace outer container:
       - Remove: `bg-gradient-to-br from-background via-background to-primary/5`
       - Add: `<AnimatedBackground />` as first child
       - Keep: `min-h-screen flex items-center justify-center p-4` but wrap content in relative div

    3. Replace CardHeader content:
       - Remove CardTitle and CardDescription
       - Add AuthHeader with:
         - title: "Aan de slag!"
         - subtitle: "Maak een nieuw HabitStreak account"

    4. Apply glassmorphism to Card:
       - Add: `glass-strong` class
       - Keep: `w-full max-w-md shadow-xl animate-scale-in`

    5. Ensure form validation and error handling remain unchanged

    6. Keep all Dutch text
  </action>
  <verify>npm run build succeeds; signup page loads at /signup with branding visible</verify>
  <done>Signup page shows flame icon, "HabitStreak" name, "Aan de slag!" message, and glassmorphism card</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Login page at /login shows:
  - [ ] Animated background with floating orbs
  - [ ] Flame icon with HabitStreak branding
  - [ ] "Welkom terug!" welcoming message
  - [ ] Glassmorphism card styling
- [ ] Signup page at /signup shows:
  - [ ] Animated background with floating orbs
  - [ ] Flame icon with HabitStreak branding
  - [ ] "Aan de slag!" welcoming message
  - [ ] Glassmorphism card styling
- [ ] Login form still works (can log in)
- [ ] Signup form still works (can create account)
- [ ] Pages are mobile responsive
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- LOGIN-01 satisfied: Welcoming message visible on login page
- LOGIN-02 satisfied: HabitStreak branding with flame icon visible
- LOGIN-03 satisfied: Glassmorphism styling matches landing page
</success_criteria>

<output>
After completion, create `.planning/phases/12-login-polish/12-01-SUMMARY.md`
</output>
