---
phase: 05-polish-consistency
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/(main)/vandaag/page.tsx
  - src/app/(main)/inzichten/page.tsx
  - src/app/(main)/taken/page.tsx
  - src/app/(main)/instellingen/page.tsx
  - src/components/navigation/bottom-nav.tsx
autonomous: false

must_haves:
  truths:
    - "User experiences consistent animation timing across all screens"
    - "User sees consistent glass card styling on all pages"
    - "User sees consistent spacing/padding on all pages"
    - "User sees consistent touch targets on interactive elements"
  artifacts:
    - path: "src/app/(main)/vandaag/page.tsx"
      provides: "Today page with consistent styling"
    - path: "src/app/(main)/inzichten/page.tsx"
      provides: "Insights page with consistent styling"
    - path: "src/components/navigation/bottom-nav.tsx"
      provides: "Navigation with consistent styling"
  key_links:
    - from: "all pages"
      to: "glass utility classes"
      via: "Tailwind classes"
      pattern: "glass"
---

<objective>
Audit and unify visual personality across all screens to deliver VISUAL-03.

Purpose: Ensure the playful personality established in Phases 2-4 is applied consistently across ALL screens - not just the today page. This completes the UI refresh by making every screen feel part of the same cohesive experience.

Output: All main screens audited for consistency with targeted fixes applied, verified by human review.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-polish-consistency/05-01-SUMMARY.md
@.planning/phases/05-polish-consistency/05-RESEARCH.md

@src/app/(main)/vandaag/page.tsx
@src/app/(main)/inzichten/page.tsx
@src/app/(main)/taken/page.tsx
@src/app/(main)/instellingen/page.tsx
@src/components/navigation/bottom-nav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all main pages for consistency issues</name>
  <files>N/A - audit only</files>
  <action>
Read and analyze each main page for consistency against the established patterns from Phases 2-4:

**Consistency checklist (from 05-RESEARCH.md):**

### Animation Timing
- All fade-ins use consistent duration (300ms via animate-fade-in or animate-slide-up)
- Page content uses PageTransition component
- Button press uses same scale (active:scale-[0.97])

### Color Usage
- Primary color for interactive elements
- Muted-foreground for secondary text
- Consistent success/error colors

### Spacing
- Card padding consistent (p-4 or p-6)
- Section gaps consistent (space-y-6 or gap-6)
- Touch targets minimum 44px (touch-target class)

### Glass Effects
- Cards using glass class where appropriate
- Consistent border styling

### Personality Elements
- Loading states show shimmer
- Empty states have friendly messaging (Dutch)

Document any inconsistencies found, organized by page.
  </action>
  <verify>Document created listing inconsistencies by page</verify>
  <done>Audit complete with documented inconsistencies</done>
</task>

<task type="auto">
  <name>Task 2: Apply targeted fixes to inconsistent pages</name>
  <files>src/app/(main)/vandaag/page.tsx, src/app/(main)/inzichten/page.tsx, src/app/(main)/taken/page.tsx, src/app/(main)/instellingen/page.tsx, src/components/navigation/bottom-nav.tsx</files>
  <action>
Based on audit findings, apply targeted fixes:

**Common fixes (apply as needed):**

1. **Missing glass styling**: Add `glass` class to Card components that should have glassmorphism effect

2. **Inconsistent page entry animation**: Ensure all pages wrap content in `<PageTransition>` or use `animate-fade-in` / `animate-slide-up` on container

3. **Missing touch-target class**: Add to buttons and interactive elements that need 44px minimum

4. **Spacing inconsistencies**: Standardize to:
   - Page container: `py-6 px-4`
   - Section gaps: `space-y-6`
   - Card padding: `p-4` or `p-6`

5. **Button styling**: Ensure interactive buttons have `active:scale-[0.97] active:brightness-95`

6. **Bottom nav glass effect**: If not already present, add glass styling for visual integration with new backgrounds

**DO NOT:**
- Over-engineer or add new features
- Change functionality, only styling
- Add animations beyond what's established
- Refactor working code unnecessarily

Keep changes minimal and targeted.
  </action>
  <verify>npm run build succeeds, npm run lint passes</verify>
  <done>Targeted fixes applied to all pages with inconsistencies</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Dynamic animated backgrounds (Plan 05-01) + visual consistency fixes across all screens (this plan).

    The entire Phase 5 deliverable is now complete:
    - Floating gradient orb background on all authenticated pages
    - Theme-aware colors (blue/pink)
    - Dark mode support
    - Reduced motion support
    - Consistent styling across all main screens
  </what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Login to the app

    **Animated Backgrounds (VISUAL-02):**
    3. Visit /vandaag - Observe subtle floating gradient orbs behind content
    4. Switch to pink theme in /instellingen - Orbs should change to pink tones
    5. Toggle dark mode - Orbs should be slightly dimmer
    6. Enable "Reduce motion" in OS settings - Orbs should stop floating

    **Visual Consistency (VISUAL-03):**
    7. Navigate through all pages (/vandaag, /inzichten, /taken, /instellingen)
    8. Check each page for:
       - Consistent card styling (glass effect)
       - Consistent spacing and padding
       - Consistent entry animations
       - Touch targets feel properly sized
    9. Check bottom navigation styling

    **Overall Personality:**
    10. Does the app feel cohesive and playful?
    11. Do backgrounds enhance without distracting?
    12. Does every screen feel like part of the same experience?
  </how-to-verify>
  <resume-signal>Type "approved" if Phase 5 delivers unified playful personality, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run lint` passes
- [ ] All pages audited for consistency
- [ ] Targeted fixes applied
- [ ] Human verification checkpoint passed
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Human approved visual consistency
- No TypeScript errors
- Backgrounds render correctly
- All screens feel cohesive
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-consistency/05-02-SUMMARY.md`
</output>
